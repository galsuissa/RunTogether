<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Networking -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Bluetooth (אם נדרש לשעון; חלקן דורשות Runtime ב-Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- אל תשתמש ב-FOREGROUND_SERVICE_CONNECTED_DEVICE כדי לא לדרוש עוד הרשאות/FGS-type -->
    <!-- <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" /> -->

    <!-- נוטיפיקציות ל-FGS (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.RunTogether"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- RecommendationsService: רץ כ-FGS מסוג dataSync -->
        <service
            android:name=".RecommendationsService"
            android:exported="false"
            android:stopWithTask="true"
            android:foregroundServiceType="dataSync" />

        <!-- Activities -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.RunTogether">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity"   android:exported="false" android:theme="@style/Theme.RunTogether" />
        <activity android:name=".MainActivity"       android:exported="false" android:theme="@style/Theme.RunTogether" />
        <activity android:name=".RunActivity"        android:exported="false" android:theme="@style/Theme.RunTogether" />
        <activity android:name=".HistoryActivity"    android:exported="false" android:theme="@style/Theme.RunTogether" />
        <activity android:name=".MatchingActivity"   android:exported="false" android:theme="@style/Theme.RunTogether" />
        <activity android:name=".InvitationsActivity" android:exported="false" android:theme="@style/Theme.RunTogether" />
        <activity android:name=".ProfileActivity"    android:exported="false" android:theme="@style/Theme.RunTogether" />

    </application>
</manifest>
