<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ************ Permissions ************ -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Foreground service permissions (Android 14+ granularity) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Wear device feature -->
    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Wearable library -->
        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />

        <!-- Mark app as standalone -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <!-- ************ Main Activity ************ -->
        <activity
            android:name="com.example.wear.presentation.MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@style/MainActivityTheme.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ************ Foreground Service: HR + GPS ************ -->
        <service
            android:name="com.example.wear.presentation.HrGpsForegroundService"
            android:exported="false"
            android:foregroundServiceType="health|location" />

        <!-- ************ Wear Message Listener Service ************ -->
        <service
            android:name=".presentation.WatchMessageService"
            android:exported="true"
            android:enabled="true" />

    </application>

</manifest>
